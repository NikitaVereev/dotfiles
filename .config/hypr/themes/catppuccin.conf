# ═══════════════════════════════════════════════════════════════════════════════
# {{ meta.name }} Theme
# Generated by theme-manager.py on {{ generated_at }}
# Source: https://github.com/morhetz/gruvbox
# ═══════════════════════════════════════════════════════════════════════════════

# Borders
$active_border   = rgba({{ accent_green }}ee) rgba({{ accent_aqua }}ee) 45deg
$inactive_border = rgba({{ ui_border }}99)

# Shadows
$shadow_color_active   = rgba({{ base_bg0 }}99)
$shadow_color_inactive = rgba({{ base_bg1 }}66)

# Backgrounds
$bg_color = rgba({{ base_bg0 }}ff)
$bg_alt   = rgba({{ base_bg1 }}ff)
$bg_dim   = rgba({{ base_bg0_hard }}ff)

# Foregrounds
$fg_color = rgba({{ base_fg1 }}ff)
$fg_dim   = rgba({{ base_fg3 }}ff)

# Accents
$red    = rgba({{ accent_red }}ff)
$orange = rgba({{ accent_orange }}ff)
$yellow = rgba({{ accent_yellow }}ff)
$green  = rgba({{ accent_green }}ff)
$aqua   = rgba({{ accent_aqua }}ff)
$blue   = rgba({{ accent_blue }}ff)
$purple = rgba({{ accent_purple }}ff)
$pink   = rgba({{ accent_purple }}ff)
